<?xml version="1.0" encoding="UTF-8"?>
<route xmlns="http://camel.apache.org/schema/spring">
	<from uri="switchyard://RestOrderService" />
	<log message="Message received: ${body}"/>
		<marshal>
		<jaxb prettyPrint="true"
			contextPath="pl.edu.agh.security.deps.financial.service.rest" />
	</marshal>

<!-- 	<to uri="bean://messageProcessor" />
 -->	<log message="Message received: ${body}"/>
	<to uri="switchyard://OrderService" />
</route>